# Hybrid Proof Language


After changing the grammar, run
```bash
$ pip install antlr4
$ # and some other stuff
$ cd src && antlr4 -Dlanguage=Python3 -visitor -no-listener -o parsing HybLang.g4
```


