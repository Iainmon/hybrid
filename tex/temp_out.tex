\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}



\usepackage{mikerosu}
\usepackage[a1paper, margin=4cm]{geometry}
% \usepackage[a3paper, margin=2cm]{geometry}
% \usepackage[a4paper]{geometry}
\usepackage{mathtools}
\DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}

\usepackage{amsthm}
\usepackage{marvosym}
\newtheorem{theorem}{Theorem}
\newtheorem{claim}{Claim}
\newtheorem{construction}{Construction}

\title{\LaTeX   Rendering}
\author{}
\date{February 2022}
%%%%


\begin{document}\maketitle\begin{align*}\titlecodebox{ \lib{L} }{\underline{$\subname{ctxt}(m \in \{\bit0,\bit1\}):$}\\\> $k \lar \{\bit0,\bit1\}$\\\> $c := k \oplus m$\\\> return $c$}\equiv\titlecodebox{ \lib{R} }{\underline{$\subname{ctxt}(m \in \{\bit0,\bit1\}):$}\\\> $c \lar \{\bit0,\bit1\}$\\\> return $c$}\implies&\titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{L} }{\underline{$\subname{ctxt}(m \in \{\bit0,\bit1\}):$}\\\> $k \lar \{\bit0,\bit1\}$\\\> $c := k \oplus m$\\\> return $c$}}\equiv \titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{R} }{\underline{$\subname{ctxt}(m \in \{\bit0,\bit1\}):$}\\\> $c \lar \{\bit0,\bit1\}$\\\> return $c$}}\\\titlecodebox{ \lib{real} }{\underline{$\subname{query}():$}\\\> $s \lar \{\bit0,\bit1\}$\\\> return $H(s)$}\equiv\titlecodebox{ \lib{rand} }{\underline{$\subname{query}():$}\\\> $r \lar \{\bit0,\bit1\}$\\\> return $r$}\implies&\titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{real} }{\underline{$\subname{query}():$}\\\> $s \lar \{\bit0,\bit1\}$\\\> return $H(s)$}}\equiv \titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{rand} }{\underline{$\subname{query}():$}\\\> $r \lar \{\bit0,\bit1\}$\\\> return $r$}}\\\titlecodebox{ \lib{left} }{\underline{$\subname{ctxt}(x \in \{\bit0,\bit1\}):$}\\\> $y \lar \{\bit0,\bit1\}$\\\> $z \lar \{\bit0,\bit1\}$\\\> $w := x + y + z$\\\> $q := \subname{Mod}(w, 2)$\\\> if $q \oplus q \overset{?}{=} 0$ then:\\\>\> return $w$\\\> return $w$}\equiv\titlecodebox{ \lib{right} }{\underline{$\subname{ctxt}(x \in \{\bit0,\bit1\}):$}\\\> $y \lar \{\bit0,\bit1\}$\\\> $z \lar \{\bit0,\bit1\}$\\\> return $x + y + z$}\implies&\titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{left} }{\underline{$\subname{ctxt}(x \in \{\bit0,\bit1\}):$}\\\> $y \lar \{\bit0,\bit1\}$\\\> $z \lar \{\bit0,\bit1\}$\\\> $w := x + y + z$\\\> $q := \subname{Mod}(w, 2)$\\\> if $q \oplus q \overset{?}{=} 0$ then:\\\>\> return $w$\\\> return $w$}}\equiv \titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{right} }{\underline{$\subname{ctxt}(x \in \{\bit0,\bit1\}):$}\\\> $y \lar \{\bit0,\bit1\}$\\\> $z \lar \{\bit0,\bit1\}$\\\> return $x + y + z$}}\\\titlecodebox{ \lib{real} }{\underline{$\subname{ctxt}(x, y \in \{\str{true},\str{false}\}):$}\\\> $z \lar \{\str{true},\str{false}\}$\\\> if $x \cdot y$ then:\\\>\> return $z$\\\> return $x + y$}\equiv\titlecodebox{ \lib{rand} }{\underline{$\subname{ctxt}(x, y \in \{\str{true},\str{false}\}):$}\\\> $z \lar \{\str{true},\str{false}\}$\\\> return $z$}\implies&\titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{real} }{\underline{$\subname{ctxt}(x, y \in \{\str{true},\str{false}\}):$}\\\> $z \lar \{\str{true},\str{false}\}$\\\> if $x \cdot y$ then:\\\>\> return $z$\\\> return $x + y$}}\equiv \titlecodebox{\fancy{A} }{$m \lar \{\bit0,\bit1\}$\\$c := \subname{ctxt}(m)$\\$j \lar \{\str{a},\str{b},\str{c}\}$\\return $c \oplus k || j$}\link\glowingbox{\titlecodebox{ \lib{rand} }{\underline{$\subname{ctxt}(x, y \in \{\str{true},\str{false}\}):$}\\\> $z \lar \{\str{true},\str{false}\}$\\\> return $z$}}\\\end{align*}\end{document}